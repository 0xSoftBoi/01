interface:
  display_name: "01"
  short_description: "Repo-specific patterns and workflows for 01"
  default_prompt: "Use the 01 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true